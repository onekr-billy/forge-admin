<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.system.mapper.SysUserPostMapper">

    <select id="selectActivePostIdsByUser" resultType="java.lang.Long">
        SELECT DISTINCT up.post_id
        FROM sys_user_post up
        INNER JOIN sys_post p
            ON p.id = up.post_id
           AND p.tenant_id = up.tenant_id
           AND p.del_flag = 0
        WHERE up.user_id = #{userId}
          AND up.tenant_id = #{tenantId}
    </select>

</mapper>
