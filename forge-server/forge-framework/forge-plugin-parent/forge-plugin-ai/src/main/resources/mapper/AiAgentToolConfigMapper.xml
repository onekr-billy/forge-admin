<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.ai.agenttool.mapper.AiAgentToolConfigMapper">
  <sql id="toolColumns">
    id, tenant_id, agent_id, tool_source, tool_key, tool_group, enabled,
    create_by, create_time, create_dept, update_by, update_time, del_flag
  </sql>

  <select id="selectToolConfigPage" resultType="com.mdframe.forge.plugin.ai.agenttool.domain.AiAgentToolConfig">
    SELECT <include refid="toolColumns" />
    FROM ai_agent_tool_config
    WHERE del_flag = 0
    <if test="agentId != null">
      AND agent_id = #{agentId}
    </if>
    <if test="toolSource != null and toolSource != ''">
      AND tool_source = #{toolSource}
    </if>
    <if test="keyword != null and keyword != ''">
      AND tool_key LIKE CONCAT('%', #{keyword}, '%')
    </if>
    ORDER BY create_time DESC
  </select>

  <select id="countByAgentAndKey" resultType="int">
    SELECT COUNT(1) FROM ai_agent_tool_config
    WHERE del_flag = 0 AND agent_id = #{agentId}
      AND tool_source = #{toolSource} AND tool_key = #{toolKey}
    <if test="excludeId != null">
      AND id != #{excludeId}
    </if>
  </select>

  <!-- 按 Agent 查询已启用(enabled='1')的工具绑定，供引擎运行时声明工具使用 -->
  <select id="selectEnabledByAgentId" resultType="com.mdframe.forge.plugin.ai.agenttool.domain.AiAgentToolConfig">
    SELECT <include refid="toolColumns" />
    FROM ai_agent_tool_config
    WHERE del_flag = 0 AND agent_id = #{agentId} AND enabled = '1'
    ORDER BY create_time ASC
  </select>
</mapper>
