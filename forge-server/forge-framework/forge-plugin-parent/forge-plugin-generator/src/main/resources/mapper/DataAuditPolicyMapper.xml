<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.generator.mapper.DataAuditPolicyMapper">

    <sql id="Base_Columns">
        id, tenant_id, object_id, enabled, reason_required, show_in_detail, policy_version, enabled_at,
        write_barrier, coverage_status, coverage_json, policy_snapshot, del_flag,
        create_by, create_time, create_dept, update_by, update_time
    </sql>

    <select id="selectByObjectId" resultType="com.mdframe.forge.plugin.generator.domain.entity.AiDataAuditPolicy">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_data_audit_policy
        WHERE tenant_id = #{tenantId}
          AND object_id = #{objectId}
          AND del_flag = 0
        LIMIT 1
    </select>

    <!-- 唯一键等值查询禁用 LIMIT 1：租户拦截器会把 LIMIT+FOR UPDATE 重排成非法的 FOR UPDATE LIMIT -->
    <select id="selectByObjectIdForUpdate" resultType="com.mdframe.forge.plugin.generator.domain.entity.AiDataAuditPolicy">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_data_audit_policy
        WHERE tenant_id = #{tenantId}
          AND object_id = #{objectId}
          AND del_flag = 0
        FOR UPDATE
    </select>

    <select id="selectConfigured" resultType="com.mdframe.forge.plugin.generator.domain.entity.AiDataAuditPolicy">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_data_audit_policy
        WHERE tenant_id = #{tenantId}
          AND del_flag = 0
    </select>

    <select id="selectAllEnabled" resultType="com.mdframe.forge.plugin.generator.domain.entity.AiDataAuditPolicy">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_data_audit_policy
        WHERE enabled = 1
          AND del_flag = 0
    </select>
</mapper>
