<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.generator.mapper.DataAuditCursorMapper">

    <sql id="Base_Columns">
        id, tenant_id, object_id, record_id, revision, last_event_id,
        create_by, create_time, create_dept, update_by, update_time
    </sql>

    <select id="selectByRecord" resultType="com.mdframe.forge.plugin.generator.domain.entity.AiDataAuditCursor">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_data_audit_cursor
        WHERE tenant_id = #{tenantId}
          AND object_id = #{objectId}
          AND record_id = #{recordId}
        LIMIT 1
    </select>

    <!-- 唯一键等值查询禁用 LIMIT 1：租户拦截器会把 LIMIT+FOR UPDATE 重排成非法的 FOR UPDATE LIMIT -->
    <select id="selectByRecordForUpdate" resultType="com.mdframe.forge.plugin.generator.domain.entity.AiDataAuditCursor">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_data_audit_cursor
        WHERE tenant_id = #{tenantId}
          AND object_id = #{objectId}
          AND record_id = #{recordId}
        FOR UPDATE
    </select>

    <update id="updateRevision">
        UPDATE ai_data_audit_cursor
        SET revision = #{revision},
            last_event_id = #{lastEventId},
            update_time = NOW()
        WHERE id = #{id}
    </update>
</mapper>
