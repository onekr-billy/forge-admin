<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.generator.mapper.DataAuditScopeMapper">

    <sql id="Base_Columns">
        id, tenant_id, role_id, object_id, object_code, object_name, del_flag,
        create_by, create_time, create_dept, update_by, update_time
    </sql>

    <select id="selectByRoleId" resultType="com.mdframe.forge.plugin.generator.domain.entity.AiDataAuditScope">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_data_audit_scope
        WHERE tenant_id = #{tenantId}
          AND role_id = #{roleId}
          AND del_flag = 0
        ORDER BY id ASC
    </select>

    <select id="selectByRoleIds" resultType="com.mdframe.forge.plugin.generator.domain.entity.AiDataAuditScope">
        SELECT
        <include refid="Base_Columns"/>
        FROM ai_data_audit_scope
        WHERE tenant_id = #{tenantId}
          AND del_flag = 0
        <if test="roleIds != null and roleIds.size() > 0">
            AND role_id IN
            <foreach collection="roleIds" item="rid" open="(" separator="," close=")">
                #{rid}
            </foreach>
        </if>
        <if test="roleIds == null or roleIds.size() == 0">
            AND 1 = 0
        </if>
        ORDER BY id ASC
    </select>

    <update id="logicDeleteByRoleId">
        UPDATE ai_data_audit_scope
        SET del_flag = id,
            update_time = NOW()
        WHERE tenant_id = #{tenantId}
          AND role_id = #{roleId}
          AND del_flag = 0
    </update>
</mapper>
