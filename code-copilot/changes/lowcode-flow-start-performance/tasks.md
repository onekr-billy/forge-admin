# 低代码流程发起性能优化 Tasks

## T1 前端非阻塞成功反馈

- [x] 为运行态快照合并和延后刷新调度补充失败测试。
- [x] 将流程运行态接口返回值立即合并到当前行/详情。
- [x] 发起、重提和撤回成功后调度后台列表校准，不再 `await` 串行刷新。
- [x] 页面 loading 在主操作返回后结束，后续仅使用列表区域 loading。

## T2 Flow 发起配置缓存

- [x] 为缓存命中、防御性拷贝、模型变更和租户隔离补充失败测试。
- [x] 提取发起配置解析方法，实现 30 秒、最多 512 项的租户级内存缓存。
- [x] 使用模型指纹在 XML/版本/更新时间变更后立即绕过旧缓存。
- [x] 缓存读写执行深拷贝，避免共享可变 DTO。

## T3 增量验证

- [x] 执行前端业务动作定向 Vitest 与定向 ESLint。
- [x] 执行 Flow 模型服务定向单测和插件编译。
- [x] 执行前端生产构建和 `git diff --check`。
- [x] 将命令、结果和环境边界追加到 `execution-log.md`。
