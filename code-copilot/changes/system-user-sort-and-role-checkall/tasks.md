# 角色授权菜单目录树任务

- [x] 保留菜单资源树的目录层级，并将页面入口作为目录下的叶子节点。
- [x] 隐藏功能页继续携带原始 `resourceId` 参与授权，只在界面标注“导航隐藏”。
- [x] 目录选择展示后代页面，页面选择展示单页；当前目录批量授权仅作用于当前目录范围。
- [x] 拆出 `RolePermissionNavigation.vue` 和 `role-permission-model.js`，避免继续扩大巨型权限配置组件。
- [x] 增加嵌套目录、隐藏页面和搜索上下文单测。
- [x] 完成 ESLint、目标 Vitest、`git diff --check` 和前端生产构建验证。
- [x] 解耦页面入口与 API/按钮授权，保存时仅为明确页面入口补齐父级菜单链。
- [x] 增加 API-only 角色资源保存回归测试并完成后端插件模块编译验证。
