# 低代码审批流程衔接优化 Tasks

## T1 设计器生命周期收敛

- [x] 删除审批节点配置组件内部的 `FlowDesignPage` 和全屏遮罩。
- [x] 保留单一 `openFlowDesigner` 事件，由业务流程工作台统一打开和关闭。
- [x] 保存/部署时刷新目录但不误关设计器。

## T2 新建模型绑定与回显

- [x] 新建成功后立即写入模型 ID、Key、名称和默认表单绑定。
- [x] 合并当前草稿模型与服务端目录，明确显示待发布状态。
- [x] 部署/关闭后刷新目录并保持当前模型选择和预览。

## T3 可设计模型目录协议

- [x] Flow 模型服务新增按可选状态查询的租户目录。
- [x] `/api/flow/model/list` 默认兼容已发布目录，并支持显式 `includeDraft=true`。
- [x] 内部 `FlowClient` 的可设计目录请求显式包含草稿，发布目录仍传 `status=1`。

## T4 回归测试与验证

- [x] 前端补充“单层打开、新建即绑定、草稿回显、部署刷新”回归测试。
- [x] 后端补充列表协议和服务查询测试。
- [ ] 前端定向测试、构建和静态检查已执行；后端测试/编译因本机无 JDK/Maven 未执行。
- [x] 将执行证据回填 `execution-log.md`。
