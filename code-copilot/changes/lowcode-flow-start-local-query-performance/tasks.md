# 低代码流程发起本地查询优化 Tasks

## T1 轻量发起校验

- [x] 为预加载校验补充失败测试，证明允许发起、状态拒绝、既有实例拒绝和权限拒绝行为。
- [x] 抽取统一的运行态发起判定，保留旧校验入口兼容。
- [x] 新增预加载校验入口，禁止读取完整详情运行态依赖。

## T2 发起上下文查询复用

- [x] 为发起方法补充性能契约测试，约束一次最新关联查询和预加载校验。
- [x] 复用已解析流程绑定构建单据配置视图。
- [x] 复用运行配置和单据配置补齐业务绑定默认值。
- [x] 复用最新流程关联计算运行中判断、Flow 业务 Key 和 `roundNo`。

## T3 增量验证

- [x] 读取自动化测试规范、项目偏好和流程/低代码踩坑记录。
- [x] 使用 JDK 17 执行轻量校验与性能契约定向测试。
- [x] 使用 JDK 17 编译 generator 插件及上游 reactor；reactor 到达 generator 后被两处无关工作区错误阻塞。
- [x] 执行 `git diff --check` 并记录真实环境边界。
