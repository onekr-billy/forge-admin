# 低代码应用设计体验与页面形态优化 Tasks

## 阶段一（本轮）

- [x] T1.1 梳理并锁定 `objectCode` 的租户级唯一作用域和冲突错误协议；当前页面没有独立持久化 `pageKey`，不重复引入第二套编码。
- [x] T1.2 在业务对象 Mapper/Service 增加租户范围唯一查询与保存前校验，编辑时排除自身。
- [x] T1.3 在页面/对象创建前端增加异步唯一校验，避免重复请求并兼容后端冲突响应。
- [x] T1.4 修复新建页面到设计器的空白过渡，补齐设计器占位和加载阶段高度约束。
- [x] T1.5 增加阶段一前端/后端定向测试，运行构建并记录证据。

## 阶段二

- [x] T2.1 统一 `treeConfig` 旧字段到业务字段的归一化函数和兼容测试。
- [x] T2.2 检查并修复列表设计器模板切换、保存和重新打开逻辑。
- [x] T2.3 将树配置面板改为业务化字段名称，并补充数据来源/节点取值/右表过滤说明。
- [x] T2.4 补齐 `apiConfig.tree` 生成和发布版本持久化。

## 阶段三

- [x] T3.1 固化树接口全量/懒加载请求参数和响应协议。
- [x] T3.2 修复 TreeCrudTemplate 节点值、右表过滤字段分离及空态/错误态。
- [x] T3.3 补充 DynamicCrudController/Service 的树查询与租户、数据权限约束。
- [x] T3.4 使用接口级和组件级测试验证节点过滤、懒加载和全量加载；并接通正式 PortalPageRenderer 的树事件与右表过滤。

## 阶段四

- [x] T4.1 统一子表展示字段和筛选条件顺序 schema。
- [x] T4.2 在设计器中加入竖向拖拽排序和键盘上下移动。
- [x] T4.3 让运行态 CRUD、表单预览和流程任务表单复用同一排序结果。
- [x] T4.4 增加旧配置迁移、非法字段回退和顺序稳定性测试。

## 阶段五（后续专项：流程状态与巨型类治理）

- [ ] T5.1 为 `flowStatus` 建立“流程绑定 → 对象草稿 → 列表选列 → 发布版本 → 运行配置”的契约测试，复现整列消失场景。
- [ ] T5.2 抽取 `FlowStatusRepairService`，保证绑定/发布/运行时对托管字段使用同一修复策略，并尊重用户显式隐藏配置。
- [ ] T5.3 抽取 `RuntimeTreeConfigBuilder`、`RuntimeChildTableCompiler`，冻结低代码运行配置协议。
- [ ] T5.4 按 `giant-class-audit.md` 分批拆分流程服务、列表设计器和属性面板，每次拆分单独编译与回归。
