# 增量验证

本轮首次建立文件页验证基线，复用 data-scope-usability 的独立 Vite + Playwright 验证方式。

## P0

- 优先 Node 20.19.0 下运行本页/新增组件/store 的定向 ESLint 与 pnpm build；本机该版本缺失，实际使用已安装的 Node 24.21.0 及相同的 ESLint/Vite 入口完成验证。
- 浏览器验证分类、文件名搜索、低频筛选、清除筛选、分页参数、列表/网格切换保留筛选。
- 验证文件菜单、重命名/分组/移动弹窗、删除取消不发出写请求，预览、下载、复制及上传契约使用模拟响应。
- 验证空态、错误重试、快速分类切换不会被过时响应覆盖。

## P1

- 桌面亮色、暗色、390px 窄屏截图并检查横向溢出/内容滚动边界。
- git diff --check；单组件行数不超过 800；不改无关现有工作区内容。

## 跳过范围

- 不启动真实后端或修改真实文件数据；真实存储服务、签名 URL、文件上传/下载后端链路留作运行环境验收。

## 本轮结果

- 页面、5 个同域组件、utils 与 Pinia store 已通过定向 ESLint。
- 生产构建通过，9236 个模块；所有组件均小于 800 行。
- Playwright + Chrome 模拟 API 验证通过，44 次请求、0 个浏览器运行时异常，保留 11 张验证截图。
- 空态、错误重试、竞态、删除取消、长字符串分组 ID、搜索/分类/分页/视图共用数据、重命名/移动/添加分组及上传载荷均已覆盖。

## 2026-09-17 推送前增量验证

同步远端 main 的 a856ecab 后，复用已有浏览器验收，重新执行变更文件 ESLint、生产模式构建及 git diff --check；同时复跑动态导入恢复的现有 4 个单测，验证两组本地改动与新远端基线可以一起提交。
