# 低代码单据与审批流程生命周期同步 Tasks

## T1 驳回语义与 BPMN 回路

- [x] 为 `allowRejectToStart` 生成独立的发起人修改条件分支。
- [x] 普通审批动作显式清理 `rejectToStart`，避免变量跨节点残留。
- [x] 补充 `TO_END`、`MANUAL` 和已有驳回分支场景的转换测试。
- [x] 调整设计器和待办页文案，说明普通驳回与退回发起人的区别。

## T2 运行态状态机

- [x] 将 `NEED_MODIFY` 纳入所有运行态判断和关联查询。
- [x] 任务创建、任务完成和待办动作返回同步更新关联状态与单据状态。
- [x] 发起人修改节点读取时按任务定义键兜底识别并提供重提入口。
- [x] 保证终态不被延迟任务事件覆盖。

## T3 单据按钮与撤回同步

- [x] 单据存在主流程实例后隐藏 `START_FLOW`，覆盖待修改和所有终态。
- [x] 默认 `REJECTED/CANCELED` 状态关闭再次发起能力。
- [x] 撤回后同步 `CANCELED` 并刷新列表/详情运行态。
- [x] 补充前端启动动作显隐和后端运行态动作测试。

## T4 验证

- [x] 执行前端转换器与业务动作定向测试。
- [x] 执行 Generator/Flow 定向后端测试与相关模块编译。
- [x] 执行前端构建、`git diff --check` 和关键静态搜索。
- [x] 将验证证据记录到 `execution-log.md`。

## T5 终态应用流程与操作反馈

- [x] 运行态批量返回已占用启动入口的应用级流程编码。
- [x] 静态 `START_PROCESS` 按 `processCode` 隐藏已运行、已成功或已撤回的流程。
- [x] 撤回确认文案改为终态语义，不暗示可再次发起。
- [x] 发起、修改后重提和撤回增加动态页面 loading 反馈。
- [x] 补充前后端回归测试并更新执行记录。

## T6 新版应用撤回与编辑性能

- [x] `BusinessDocumentRuntimeService` 在无旧配置时支持撤回，覆盖详情与批量、发起人/非发起人/终态/权限反例。
- [x] `DynamicCrudRepository` 后台审计填充兼容无 Web 上下文，`BusinessFlowService` 状态回写失败向上传播；增加无请求回调与失败回归。
- [x] `crud/composables/useRecordFormLoader.js` 抽出编辑/详情加载，并行获取详情及运行态，防止旧运行态覆盖。
- [x] 定向 Maven、Vitest、ESLint、生产构建并追加证据；不重启服务或操作真实流程。
- [ ] 人工验收：重启 Admin 后用测试记录验证两个撤回入口；已失败历史事件按实际引擎状态单独补偿，本轮未执行数据修复。
