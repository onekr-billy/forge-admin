# 增量验证计划

## 应用业务流程开放增量
- P0：无旧 FLOW 绑定但应用快照包含手动业务流程，可枚举/发布并调用原编排器；固定流程版本校验，无任意模型/用户/变量覆盖。
- P0：跨应用/租户/对象、草稿新增、未发布/停用/版本漂移、无手动节点、无权限/记录不可见拒绝；失败运行不会由外部重放自动 retry。
- P1：应用/页面 → 业务流程自动或显式选择，不调用旧 getFlowActionRegistrationSource；局部加载、异常重试、换页面/关闭丢弃旧请求，升级固定来源。
- 复用现有定向 POM/Vitest，补充编排器版本边界测试，JDK17 Admin 聚合构建、定向 ESLint、Vite 构建及真实组件模拟 API 浏览器验证。用户负责真实 Admin/Flow/MySQL/审批 E2E。

### 风险等级发布契约回归（2026-09-22）
- P0：应用业务流程注册来源必须输出 `SYSTEM_SERVICE / ACTION / MEDIUM` 对应的 `MEDIUM` 风险等级，经 `SystemServiceCapabilityPublisher` 组装后可通过目录层受控系统服务元数据校验。
- P0：不放宽 `CapabilityCatalogService.publishSystemService` 和客户端授权对高风险能力的限制；应用、流程、记录、用户与权限固定校验保持不变。
- 先运行应用流程服务定向测试复现 `HIGH` 断言失败，再修复并复跑开放平台 verification；共享后端代码改动后执行 Admin 聚合构建和 `git diff --check`。本轮无前端、数据库或 Flyway 变更，不重复前端构建及真实流程 E2E。

### 本轮结果（2026-09-22 19:26）
- 修复前应用流程定向测试 17 项中新增契约断言 1 项失败，明确得到 `expected MEDIUM but was HIGH`；不是应用或流程未发布问题。
- 修复后 Admin 46 模块聚合安装 `BUILD SUCCESS`（24.786s）；补齐最终发布器断言后开放平台 verification 86/86 通过（5.873s），其中应用流程服务 17/17、系统服务发布器 4/4 通过。
- 未修改目录层高风险发布/授权边界，未修改数据库、Flyway、前端或既有能力版本；未启动 Admin/Flow，也未执行真实网关和审批 E2E。

## 注册、归属与授权闭环增量
- P0：应用内进入表单填报时不调用全量业务对象/全量表单来源；页面选择完成后只携带字符串 `applicationId/objectId` 请求 `lowcode.form.create`，后端只解析该对象，不能调用 `BusinessObjectService.list`。
- P0：新建注册不出现 `FLOW_ACTION/旧版对象审批`；应用业务流程仍输出 `MEDIUM` 并可通过受控目录校验，存量 FLOW_ACTION 升级恢复不受影响。
- P0：目录入口选择应用页面发布时走应用 publish 路由；应用同步只关联当前发布快照中的对象来源和固定 applicationId 流程，跨应用、任意 REST、坏 JSON、未发布版本不关联。重复同步/重复 attach 幂等。
- P0：SYSTEM_SERVICE 授权接受缺失、JSON null、空对象并落 NULL；非空字段/操作策略、数组及标量仍拒绝。覆盖全局授权与应用内授权请求。
- P1：身份来源改用普通单选，仍能切换 USER_ASSERTION/OIDC；加载、切换、关闭的竞态测试继续通过。执行定向 Vitest/ESLint/Vite build、开放平台与应用集成 verification、Admin 聚合构建、`git diff --check`；不执行真实数据库、网关或审批 E2E。

### 2026-09-22 注册、归属与授权闭环结果
- 前端受影响的 3 个测试文件 41/41 通过；定向 ESLint 0 errors / 0 warnings；Vite 生产构建通过（26.79s），保留既有工具链提示。
- 开放平台 verification 96/96 通过，覆盖应用流程 `ACTION / MEDIUM` 发布契约、单对象表单来源和系统服务授权的 null/空对象策略；应用集成 verification 47/47 通过，覆盖已有目录能力认领、存量同源候选只认领一次、跨应用/坏策略/REST 排除及同来源重复注册拒绝。
- JDK 17 Admin reactor 46 模块 `BUILD SUCCESS`（20.795s）；Mapper XML 解析和 `git diff --check` 通过。
- 未启动 Admin/Flow、未连接 MySQL 或开放网关；同步和幂等行为以 Mapper/服务测试验证，不替代真实事务、网关鉴权与审批 E2E。

### 2026-09-22 最终结果
- 前端 13 文件、115/115 通过（18:07:09，2.82s）；7 个修改/新增 JS/Vue 文件 ESLint 通过，无 errors/warnings。
- 开放平台 verification：86/86 通过（18:03:33，5.333s），含新应用流程服务 17 项及真实编排器 11 项；应用集成 verification：43/43 通过（18:03:28，4.038s），含应用内跨应用发布参数拦截。均无 failures/errors/skipped。
- Admin 46 模块 JDK17 reactor BUILD SUCCESS（18:02:36，24.570s）；Vite 生产构建通过（33.17s），保留已有 CSS/chunk/plugin timing 警告。
- 浏览器使用真实 Vue 组件、模拟 API 及列表外壳，完整点击应用内注册 → 流程操作 → 应用业务流程 → 页面 → 流程 → 发布确认 → 模拟发布返回列表；2 秒来源延迟时立即 loading，多流程需选择。390px 深色 width/scrollWidth 均 390，console error=[]。
- 服务/数据库使用 mock，不等价于真实 Flowable 审批、网关鉴权、数据库事务或消息投递验收。已关闭本轮临时页面、恢复视口并停止本轮 localhost:5199 服务。

## 应用内能力来源误判增量
- P0：真实 SnapshotService.prepare 生成快照，经真实 RuntimeService 读取，再执行 ApplicationIntegrationService.requireSource；不得只 mock 一个手工补齐 suiteCode 的运行 VO。
- P0：旧快照缺 suiteCode 时，精确对象 ID 仍可注册；新快照保留对象原套件，套件冲突不得回退。
- P0：同编码跨套件、仅存在于草稿、对象删除或重建、缺 ID、空来源、租户缺失及门户无权限拒绝；来源查询限定可信租户。
- P1：复用本变更 verification/pom.xml 和开放平台回归 POM；先红后绿，Admin 聚合构建后复跑。无前端/迁移变更，不重复 UI 构建；不启动真实服务或操作业务数据库。

### 本轮结果（2026-09-22 17:32）
- 修复前 42 项中 4 项失败：真实快照缺 suiteCode、存量快照来源误拒、未按租户解析对象身份，以及 null suiteCode 与缺字段错误相等被放行。
- 修复后应用集成 42/42（新增真实快照链路 15 项）、原开放平台 58/58，均 0 failures / errors / skipped；Admin 46 模块 BUILD SUCCESS，22.379s。
- 数据查询复用现有 XML 的 tenant_id + suite_code + object_code + del_flag 条件；测试数据库查询使用 Mockito，不声称验证真实租户拦截/HTTP/生产环境。
- 未修改前端、数据库脚本或存量快照；未启动 Admin/Flow、部署或调用真实网关。

## 自动建表开放填报增量
- P0：自动建表可发布及执行，复用页面新增协调器；只读/来源漂移拒绝，字段/租户/版本防护不回退。
- P0：主库建单与已有回执一起回滚；跨库占位先提交，失败或结果未知后相同键不得再次执行，成功同键返回原记录，不同正文冲突。
- P0：用真实 Mapper XML、Spring 事务和隔离 H2 验证回执占位并发、提交及失败边界；不冒充真实 MySQL/网关 E2E。
- P1：公共新增入口保留字段数据和创建事件，提交后派发、回滚不派发；说明不再拦截自动建表，不承诺跨库原子性/无条件重试。

### 本轮结果（2026-09-22 16:40）
- 后端白名单 12 类、58/58，通过；包含原 Controller 2 项、来源/授权 11 项、共同新增入口 4 项、真实 XML/H2 回执事务 8 项及原开放平台回归。
- H2 验证主库回滚后重试、跨库成功复用、回执失败不重复插入、异常/外层回滚保留占位、不同租户/客户端/能力隔离、同键冲突与并发保护。运行库 SQL 由测试 Supplier 模拟；不宣称验证了真实动态 CRUD 数据库链路。
- 前端 12 文件、107/107 通过；本轮仅修改支持说明，沿用前轮交互测试矩阵。定向 ESLint 通过，Vite build 27.46s，Admin 46 模块 BUILD SUCCESS 20.898s。
- XML 解析、git diff --check 通过；迁移目录、运行 JDBC provider 无差异，打包产物没有被撤回的 LowcodeFormStorage 或运行库初始化脚本。
- 未启动 Admin/Flow、执行真实 MySQL/Flyway/开放网关；本轮无新布局或交互，不重复模拟浏览器验收，也不把旧浏览器结果算作真实环境验收。

## 注册响应增量计划
- P0：指定来源时不调用无关枚举；未知 serviceCode 拒绝，旧无筛选接口兼容。
- P0：受管运行数据源仍保持事务保护、空编码主库接受；字段白名单与幂等回执回归。
- P1：点击立即 step=2/loading=true，失败可重试，切换/关闭不污染来源；场景页不全量预取。
- P1：应用内默认填报，锁定字符串应用 ID/名称，不请求全量列表；单页自动选中、多页选择，全局入口无上下文残留。
- P1：业务消息使用说明和默认折叠技术详情；明暗/窄屏真实组件夹具验证。

## 控制台细节增量
- P0：菜单路径与文件路径均能匹配真实组件；兼容地址不能绕过菜单授权，隐藏调用记录可按原授权访问。
- P0：只绑定 LOGIN 后新增 MESSAGE 无需重新绑定应用；实际发送仍检查当前平台能力/连接/应用，解绑通道停用。
- P1：连接详情嵌套 VO 回填、关闭/切换连接不接受旧请求、写入防重复、失败保留编辑、凭据编辑留空保留。
- P1：三处控制台桌面/窄屏/深色，顶部导航实际点击，保存后重新加载一致；浏览器模拟 API 不计真实服务验收。

复用 ../open-platform-experience/test-spec.md 与 execution-log.md 的 22 个前端、37 个后端测试基线，不重建全流程。

- P0：租户缺失/跨租户连接、应用外对象、应用外能力或授权 ID、未发布应用拒绝；事务覆盖发布+关联；revision 过期拒绝。
- P0：消息通道停用/解绑不降级，指定连接透传，收件人/模板语义保留。
- P1：发布来源锁定应用、注册关闭无写、列表/授权/日志强制能力范围、迟到请求隔离、防双击。
- P1：设置路由刷新与深链、亮暗主题/390px/桌面空态/错误/滚动。
- 执行定向 Vitest、ESLint、Vite build、JDK17 Maven 主编译与针对性测试、XML/SQL 静态检查、git diff --check。
- 浏览器夹具仅 mock API 验证真实组件；不替代 Flyway、真实免登/消息/开放接口 E2E。

## 本轮执行结果（2026-09-21）

| 验证 | 结果 | 范围与限制 |
| --- | --- | --- |
| 前端定向 Vitest | 62/62，通过 | 原能力注册 22 + 应用集成 store 8 + 路由/免登入口 5 + 流程设计器 27 |
| 新后端定向测试 | 20/20，通过 | service 9、controller 4、消息链路 7；含租户、过期 revision、关联归属、权限/事务注解、禁用 SSO、异步租户与幂等键 |
| 原开放平台后端回归 | 37/37，通过 | 独立 verification 项目复用已有测试源 |
| 前端定向 ESLint | 通过 | 本轮修改的前端文件，0 errors / 0 warnings |
| Vite 生产构建 | 通过 | 28.01s；保留工具链已有 CSS/chunk/plugin timing 提示，不声称全项目零警告 |
| JDK17 Admin reactor | BUILD SUCCESS | 46 模块，23.441s，跳过仓库全量测试编译 |
| XML 与差异检查 | 通过 | xmllint；git diff --check；迁移静态检查，无真实 SQL 执行 |
| 浏览器组件验收 | 通过 | 真实 Vue 组件 + mock API；连接保存、授权回填、注册应用锁定、关闭后单层、日志筛选上下文、亮暗/窄屏 |

浏览器验收在独立 localhost:5198 夹具进行，没有连接业务服务；390px 下 document.scrollWidth 与 clientWidth 均为 390，控制台 error 为空。测试完成关闭临时页和服务，恢复浏览器视口。

仓库常规后端测试编译仍受既有 DynamicCrudPrintReadTest 构造器参数不匹配、SecureActionCatalogServiceTest 缺少类型导入影响。本轮用独立 POM 编译真实新增测试源和必要依赖，没有修改这些无关测试，也未绕过本轮断言。

真实事务原子性、数据库租户拦截/Flyway、并发请求和企业微信/外部接口 E2E 尚待环境验收；注解/Mockito 单元测试不能替代这些验证。

## 本地增量收尾计划
- 构造配置旧读取晚于成功保存返回，断言新 revision 和连接保持；保存中不发起刷新。
- 构造刷新期间修改/刷新前已有未保存选择，断言用户选择不丢失。
- 构造已停用/缺失消息模板、应用通道类型被修改、错误类型开关和缺失节点 ID，断言没有实际 send 调用。
- 复用现有前端和后端定向项目执行红/绿回归；涉及共享消息执行器，重新构建 Admin 聚合，不运行真实服务。

### 2026-09-22 增量结果
- 修复前：新增前端 4 个断言场景失败（原 8 个通过）；新增后端 4 个场景失败（原 20 个通过）。失败原因分别为旧读取覆盖配置、刷新丢选择，以及未拦截错误消息配置，均已复现。
- 修复后：前端 5 文件 **66/66** 通过；本轮后端 **24/24**，原开放平台回归 **37/37** 通过。
- 定向 ESLint 0 errors / 0 warnings；Admin 46 模块 BUILD SUCCESS（22.333s）；Vite 生产构建通过（26.75s）；git diff --check 通过。
- 浏览器用同一模拟 API 夹具点击“选择连接 → 不保存直接刷新 → 选择保留 → 保存 → 入口回填”通过，控制台 error=[]。未使用真实连接或真实授权。
- Node 20.19.0 在当前环境不存在，使用已安装的 Node 24.21.0。pnpm 构建入口在依赖审批检查阶段被拦截，未放宽审批；执行 package.json 中原样的 Vite 命令完成构建。pnpm 自动产生的 workspace 审批占位项已恢复，配置文件无差异。
- 临时预览服务已停止、标签已关闭。真实数据库、迁移、企业微信/开放接口 E2E 仍未执行。

## 2026-09-22 控制台细节与配置闭环验收

| 验证 | 结果 | 边界 |
| --- | --- | --- |
| 前端定向 Vitest | 11 文件、98/98 通过，12:37:50 | 增加真实路由匹配、旧地址授权、隐藏工具页权限、连接回填、写入防重复与跨工作区迟到请求用例 |
| 后端定向测试 | 27/27，0 failures / errors | 缺 Secret、缺企业微信 AgentId、消息能力后配/停用，先复现 3 个失败用例再修复 |
| 定向 ESLint | 20 个修改/新增 JS、Vue 文件通过 | 0 errors / warnings，无自动格式化 |
| Vite build | 通过，29.95s | Node 24.21.0，原 build 脚本正文；已有工具链 CSS/chunk/timing 提示保留 |
| Admin reactor | 46 模块 BUILD SUCCESS，21.341s | JDK17，跳过全仓测试编译；独立 POM 运行上述真实测试源 |
| 差异和组件规模 | 通过 | git diff --check；修改的 SFC 均不超过 800 行 |
| 浏览器组件检查 | 通过 | 真实配置组件 + 模拟 API / 列表外壳；明暗主题、390px、桌面、导航和保存回填 |

浏览器实际点击开放平台“能力目录 → 接入系统 → 调用记录 → 返回目录”，页面均匹配；连接应用编辑后名称回显、应用能力绑定保存、应用集成保存后入口/通道回填均通过。隐藏工具页的全局权限守卫由真实 guard 单元测试覆盖，浏览器夹具没有真实登录授权链。390px 下页面宽度与滚动宽度均为 390，局部表格可横向滚动，移动端凭据表单单列且保存按钮可见。夹具最初存在两项 mock export 缺失，已修正；后续交互没有新增 error，不能把历史错误记录描述为始终为空。

夹具服务仅监听 127.0.0.1:5199，替换了 API 和 AiCrudPage 列表外壳，不声称验证全量 CRUD 或真实 HTTP/数据库事务。未启动 Admin/Flow、执行 Flyway、调用企业微信或开放网关；保留环境验收待办。历史 LOGIN-only 绑定的通道修复需升级后点击一次“重新应用配置”，已写入 usage.md。

## 2026-09-22 注册响应增量结果
- 前端 12 文件、107/107 通过（15:45:11，2.36s）：含立即 loading、按类型传参、失败重试、切换/关闭隔离、应用自动填入、页面初始化清空不挂起列表加载。
- 开放平台后端 39/39，通过（15:34:03，4.519s）：新增按 serviceCode 在枚举前筛选，以及空数据源编码接受/实际运行数据源仍拒绝测试。原应用集成后端 27 项代码未修改，本轮未重复运行。
- 9 个改动/新增前端文件 ESLint 0 errors / warnings；Admin 46 模块 BUILD SUCCESS（24.386s）；最终 Vite build 通过（28.13s），保留既有 CSS/chunk/timing 提示。
- 浏览器真实组件 + 模拟接口（人为延迟来源 6 秒）：REST/低代码选择立即显示对应加载状态；应用内跳过场景选择、自动带入应用及唯一页面、进入发布确认；消息说明和折叠编码正常；390px 深色页面 width/scrollWidth 均为 390，console error=[]。
- 自动建表的受管运行数据源实际填报仍未支持，不能计作本轮修复或数据库 E2E 通过。无真实后台服务、SQL、外部平台调用。
