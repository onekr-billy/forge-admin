# 流程管理模块优化与二次开发操作手册（草稿）

> 本文基于当前工作区最近的流程优化提交及现有改动整理，主要涉及 39ea145a、fa8bea88、32940f72、b828761f、c74f989e。
>
> 当前已完成代码编译、静态检查和定向测试，尚未启动真实 Admin/Flow 服务，也未连接 MySQL、Redis 做完整联调。

## 一、最近流程管理优化总览

| 优化内容 | 用户侧变化 | 主要代码 |
|---|---|---|
| 业务应用表单配置 | 先选业务应用，再选业务对象，最后选择应用下的表单资产 | forge-admin-ui/src/views/flow/design.vue、BusinessFlowFormAssetSelect.vue |
| 表单保存修复 | 保存时持久化 applicationId/applicationName/formKey/pageId 等完整引用 | design.vue 的表单选择和保存前校验逻辑 |
| 发起测试表单渲染 | 业务应用表单可以在“发起测试”弹窗中直接渲染并填写 | forge-admin-ui/src/views/flow/model.vue |
| 模型 Key 样式 | 输入框改为整行布局，帮助文字不再挤在输入框旁边 | model.vue、FlowModelServiceImpl.java |
| 审批退回与待办跳转 | 从模型基础弹窗移动到设计器“审批与待办”更多配置中 | design.vue、FlowTaskServiceImpl.java |
| 默认待办地址 | 默认使用 /#/pages/todo-detail?taskId={taskId} | FlowTaskNotifyListener.java |
| 业务应用绑定显示 | 已绑定应用的流程列表不再显示“未绑定业务应用” | BusinessBindingMapper.xml、model.vue |
| 待办/已办性能 | SQL 关联用户、流程模型和分类，移除列表页逐条查用户和流程变量 | FlowTaskMapper.xml、FlowTaskServiceImpl.java |
| 分页 COUNT 异常 | 自动生成的 COUNT(*) 兼容转换为 COUNT(1) | CountOnePaginationInnerInterceptor.java |

## 二、流程模型配置操作

### 1. 新增流程模型

进入“流程模型 → 新增模型”，填写流程模式、模型名称、模型 Key、流程分类和描述。

模型 Key 后端校验规则：

    ^[A-Za-z][A-Za-z0-9_-]{1,63}$

含义：

- 必须以英文字母开头
- 只能包含字母、数字、下划线和短横线
- 长度为 2～64 个字符
- 已发布或已挂起模型不允许修改 Key

实现位置：

    forge-server/forge-framework/forge-plugin-parent/forge-plugin-flow/
      src/main/java/com/mdframe/forge/starter/flow/service/impl/FlowModelServiceImpl.java

模型状态：

    0 - 设计中
    1 - 已发布
    2 - 已挂起
    3 - 已禁用

### 2. 业务流程表单配置

业务流程表单采用级联模式：

1. 选择业务应用
2. 选择该应用下的业务对象
3. 选择应用中实际配置的表单资产

前端调用链：

    businessApplicationList()
            ↓
    businessApplicationObjects(applicationId)
            ↓
    businessFlowFormAssets(objectCode, {
        includeInternal: true,
        applicationId
    })

接口：

    GET /ai/business/application/list
    GET /ai/business/application/{applicationId}/objects
    GET /ai/business/flow/form-assets/{objectCode}?applicationId=xxx&includeInternal=true

选择应用时会清空旧的业务对象和表单，避免出现“应用 A 的对象 + 应用 B 的表单”。

保存时，formJson 会保存完整表单引用：

    {
      "type": "BUSINESS_OBJECT_FORM",
      "formMode": "BUSINESS_OBJECT_FORM",
      "objectCode": "sample_purchase_order",
      "objectName": "采购单",
      "formKey": "app_1001_page_2001_form_3001",
      "formName": "采购单审批表单",
      "applicationId": "1001",
      "applicationName": "采购管理应用",
      "pageId": "2001",
      "pageCode": "purchase_apply",
      "pageName": "采购申请",
      "pageType": "FORM",
      "sourceFormKey": "purchase_apply_form",
      "viewKey": "default",
      "formRef": {
        "objectCode": "sample_purchase_order",
        "applicationId": "1001",
        "formKey": "app_1001_page_2001_form_3001"
      }
    }

保存前会执行：

- 自动补齐历史数据缺失的应用信息
- 检查是否选择了应用
- 检查是否选择了业务对象
- 检查是否选择了具体表单资产
- 不满足条件时阻止保存

相关方法：

    handleBusinessApplicationChange
    handleBusinessObjectChange
    handleBusinessGlobalFormUpdate
    normalizeBusinessGlobalFormBeforeSave
    validateBusinessGlobalFormBeforeSave

### 3. 节点表单与字段权限

流程全局表单用于发起流程，节点表单用于审批办理。

节点表单配置位于：

    forge-admin-ui/src/components/flow-designer/panel/ApproverConfig.vue
    forge-admin-ui/src/components/flow-designer/panel/PermissionConfig.vue

表单来源优先级：

    BPMN 用户任务节点配置
      formKey/formUrl/formJson/formFieldPermissions
    >
    ai_business_binding.binding_config.nodeForms
    >
    流程模型默认表单

新流程应优先在流程设计器节点抽屉中配置表单和字段权限。nodeForms 仅用于兼容历史配置。

字段权限包括是否可见、是否可编辑、是否必填和是否只读。业务表单保存时后端会再次根据节点权限过滤字段，不能只依赖前端隐藏字段。

## 三、审批人、审批动作与更多配置

### 1. 审批人类型

流程设计器支持：

- 固定用户
- 用户变量
- 发起人自选审批人
- 候选用户
- 候选组
- 多实例会签

发起人自选审批人的启动变量协议：

    {
      "PROCESS_START_USER": {
        "deptLeader": ["10001"],
        "financeApprove": ["10002", "10003"]
      }
    }

后端 InitiatorSelectedApproverSupport 负责从 BPMN 识别节点、校验审批人、生成单人节点变量 INITIATOR_SELECT_节点Key，并统一将审批人 ID 按字符串处理。

前端不得把雪花 ID 转成 JavaScript Number，应始终使用字符串。

### 2. 审批动作

    approve          审批通过
    reject           普通驳回
    rejectToStart    驳回至发起人修改路径
    return           退回上一审批节点或指定历史节点
    delegate         委派
    reassign         改派
    terminate        终结流程
    withdraw         发起人撤回
    claim            签收

主要实现：

    forge-server/forge-framework/forge-plugin-parent/forge-plugin-flow/
      src/main/java/com/mdframe/forge/starter/flow/service/impl/FlowTaskServiceImpl.java

### 3. “审批与待办”更多配置

原来放在模型基础弹窗中的审批退回和待办跳转，现统一移动到流程设计器右侧“更多配置 → 审批与待办”。

当前配置包括：

- 提交人是否允许撤回
- 是否允许指定节点驳回
- 待办跳转地址
- 自动审批模式

指定节点驳回由流程级配置 allowMultiReturn 控制，不再要求每个节点单独配置“允许退回”。

待办跳转支持：

    {taskId}
    {businessKey}
    {processInstanceId}

默认地址：

    /#/pages/todo-detail?taskId={taskId}

相对路径会自动拼接企业协同连接配置中的 H5 域名，完整 http/https 地址可直接使用。

数据库字段：

    sys_flow_model.todo_detail_url_template
    sys_flow_model.allow_multi_return

多级退回字段由 forge-server/db/migration/V1.0.134__flow_model_multi_return.sql 增加。

## 四、发起测试和历史表单

### 1. 发起测试表单渲染

“流程模型 → 更多操作 → 发起测试”会根据模型表单类型自动处理：

- 普通动态表单：使用流程动态表单渲染器
- 业务应用表单：调用业务表单资产接口后使用 AiForm
- 外置表单：根据 formUrl 加载外部页面
- 无表单：允许以空变量发起测试

业务表单字段兼容：

    fieldCatalog
    fields
    schema.fieldCatalog
    schema.fields
    schema.components

相关处理位于：

    forge-admin-ui/src/views/flow/model.vue
      loadStartTestBusinessForm
      collectStartTestFormData

外置表单字段目录通过组件导出的 flowFieldCatalog 获取：

    forge-admin-ui/src/utils/flow-form-loader.js

### 2. 已办、抄送和监控详情

已办和监控详情复用统一只读表单组件：

    forge-admin-ui/src/components/flow/FlowReadonlyFormPanel.vue
    forge-admin-ui/src/components/flow/FlowTaskDetailShell.vue

只读表单支持业务对象表单、代码表单、动态表单、外置表单、子表、审批职责、审批要点、审批记录和签名图片。

运行时接口：

    GET /api/flow/task/form/{taskId}
    GET /api/flow/task/form
    GET /ai/business/flow/task-form-context/readonly

## 五、流程状态和回调

任务状态：

    0 PENDING    待办
    1 CLAIMED    已签收
    2 APPROVED   已通过
    3 REJECTED   已驳回
    4 DELEGATED  已转办
    5 CANCELED   已取消
    6 WITHDRAWN  已撤回
    7 RETURNED   已退回
    8 TERMINATED 已终结

业务单据状态：

    NOT_STARTED
    RUNNING
    IN_PROCESS
    APPROVED
    REJECTED
    CANCELED

状态同步不再只依赖 PROCESS_COMPLETED：

- TASK_CREATED：创建待办并修复业务状态
- TASK_COMPLETED：处理节点完成、驳回、审批结果
- ON_COMPLETED：业务单据标记已通过
- ON_REJECTED：业务单据标记已驳回
- ON_CANCELED：业务单据标记已取消

流程实例关联表会保存 business_key、process_instance_id、flow_model_key、flow_status、variables_snapshot。

驳回至发起人时会保存：

    FLOW_RETURN_SOURCE_ACTIVITY_ID
    FLOW_RETURN_TARGET_ACTIVITY_ID
    FLOW_RETURN_TO_START_PENDING

发起人修改后可以正常继续流程，也可以直接送回原驳回节点。

## 六、待办、已办列表性能优化

### 1. 原来的主要问题

    分页查任务
      ↓
    循环查用户姓名
      ↓
    循环查组织/角色
      ↓
    循环读取 Flowable 流程变量
      ↓
    循环查业务单据

任务数量一多，就会产生大量 N+1 查询。

### 2. 当前实现

FlowTaskMapper.xml 增加：

- TaskListColumns
- TaskListUserJoins
- TodoCandidateCondition

列表查询直接关联：

    sys_user
    sys_user_tenant
    sys_flow_model
    sys_flow_category

一次返回 assignee_name、start_user_name、process_name、process_definition_name、category_name。

待办、已办、我发起的查询都明确带：

    t.tenant_id = #{tenantId}

候选组匹配规则：

1. 当前用户是直接处理人
2. candidate_users 包含当前用户
3. candidate_groups 匹配当前用户有效角色 ID
4. candidate_groups 匹配当前用户角色编码
5. candidate_groups 匹配当前用户当前组织 ID

同时增加当前租户、有效用户、有效角色、角色组织范围限制；没有 activeOrgId 时不匹配候选组。

列表页不再对每条任务调用 runtimeService.getVariable(processInstanceId, "businessParams")，而是从 ai_business_flow_instance_link.variables_snapshot 批量恢复业务参数。

低代码场景使用 dynamicCrudService.selectByIds(configKey, recordIds)，代码表单场景使用 provider.buildSummaries(objectCode, recordIds)，查询粒度从“每条任务一次”变成“每个业务对象一批”。

### 3. 新增联合索引

迁移脚本：

    forge-server/db/migration/V1.0.135__optimize_flow_task_list_queries.sql

索引：

    idx_flow_task_tenant_assignee_status_create
    (tenant_id, assignee, status, create_time)

    idx_flow_task_tenant_assignee_status_complete
    (tenant_id, assignee, status, complete_time)

    idx_flow_task_tenant_start_user_create
    (tenant_id, start_user_id, create_time)

脚本通过 information_schema.STATISTICS 判断，支持重复执行。

### 4. JSqlParser COUNT 异常

报错：

    net.sf.jsqlparser.parser.ParseException:
    Encountered unexpected token: "("
    at line 1, column 13

根因是复杂待办 SQL 经过分页插件、租户拦截器和数据权限拦截器后，自动生成的 SELECT COUNT(*) 无法被当前 JSqlParser 稳定解析。

CountOnePaginationInnerInterceptor 统一转换为 SELECT COUNT(1)，不需要在每个 Mapper 中手工修改分页 COUNT。

本轮主要优化了 /todo、/done、/started。/candidate 仍保留 Flowable 查询后再按任务 ID 回查的兼容逻辑，如果候选任务量很大，可以作为下一轮优化对象。

## 七、待办列表如何扩展

### 1. 增加业务摘要和展示字段

推荐使用 FlowBusinessListDisplayAdapter，不要在流程核心模块中硬编码采购单、合同、请假单等业务。

接口位置：

    forge-server/forge-flow/forge-flow-client/
      src/main/java/com/mdframe/forge/flow/client/spi/FlowBusinessListDisplayAdapter.java

展示对象：

    forge-server/forge-flow/forge-flow-client/
      src/main/java/com/mdframe/forge/flow/client/spi/FlowBusinessListDisplayItem.java

扩展示例：

    @Component
    @RequiredArgsConstructor
    public class ContractFlowListDisplayAdapter
            implements FlowBusinessListDisplayAdapter {

        private final ContractMapper contractMapper;

        @Override
        public void enrich(List<FlowBusinessListDisplayItem> items) {
            List<Long> recordIds = items.stream()
                    .map(FlowBusinessListDisplayItem::getRecordId)
                    .filter(Objects::nonNull)
                    .distinct()
                    .toList();

            Map<Long, Contract> records = contractMapper.selectMapByIds(recordIds);

            for (FlowBusinessListDisplayItem item : items) {
                Contract contract = records.get(item.getRecordId());
                if (contract == null) {
                    continue;
                }

                item.setBusinessObjectName("合同");
                item.setBusinessSummary(contract.getContractName());
                item.setDisplayExtensions(Map.of(
                        "fields", List.of(
                                Map.of("label", "合同编号", "value", contract.getContractNo()),
                                Map.of("label", "金额", "value", contract.getAmount())
                        )
                ));
            }
        }
    }

推荐展示协议：

    {
      "fields": [
        { "label": "合同编号", "value": "HT20260001" },
        { "label": "金额", "value": "120000" }
      ]
    }

前端统一由 FlowTaskCardList.vue、FlowTaskBusinessSummary.vue 和 views/flow/utils/processDisplay.js 渲染。

列表页使用插槽：

    <template #summary="{ row }">
      <FlowTaskBusinessSummary :row="row" />
    </template>

不要在 Vue 的 v-for 中逐条请求业务详情。

### 2. 增加待办筛选条件

例如增加业务类型、发起人、业务编号、时间范围等筛选条件，需要同时修改：

1. FlowTaskController
2. FlowTaskService
3. FlowTaskMapper
4. FlowTaskMapper.xml
5. todo.vue / done.vue 查询模型

复杂条件写在 Mapper XML：

    <if test="businessType != null and businessType != ''">
        AND t.business_type = #{businessType}
    </if>

新增条件后同步检查租户条件、索引、分页 COUNT 和 SQL 合同测试。

### 3. 增加列表列

FlowTaskCardList.vue 已提供以下插槽：

    #title
    #summary
    #status
    #node
    #user
    #actions
    #filters
    #batch-actions

具体业务字段优先放入 businessSummary、displayExtensions、businessParams，不建议直接修改流程核心实体。

## 八、代码优先业务流程二开

适用于采购、合同、付款、请假等需要自定义 Service、状态机和业务副作用的复杂业务。

推荐目录：

    业务模块
    ├── domain/Xxx.java
    ├── dto/XxxDTO.java
    ├── dto/XxxTaskSaveDTO.java
    ├── vo/XxxVO.java
    ├── mapper/XxxMapper.java
    ├── resources/mapper/business/XxxMapper.xml
    ├── service/XxxService.java
    ├── service/impl/XxxServiceImpl.java
    ├── provider/XxxCodeFormProvider.java
    ├── support/XxxFlowDefinition.java
    └── support/XxxFlowBpmn.java

实现 BusinessCodeFormProvider 的核心方法：

    String providerKey();
    String providerName();
    List<Map<String, Object>> formAssets(String objectCode);
    BusinessTaskFormContextVO buildContext(...);
    BusinessTaskFormContextVO saveContext(...);
    Map<Long, String> buildSummaries(String objectCode, Collection<Long> recordIds);

Provider 负责注册表单资产、提供字段目录、构建待办/只读上下文、保存节点允许编辑的字段、批量构建列表摘要和校验业务状态。

业务 Service 负责创建单据、发起流程、保存节点字段、接收回调、修复状态和执行幂等副作用。

流程业务 Key 统一使用：

    <objectCode>:<recordId>

例如：

    sample_purchase_order:10001

## 九、低代码业务流程二开

适用于业务对象、业务应用和表单均由平台配置的场景。

典型绑定配置：

    {
      "flowModelKey": "purchase_approval",
      "titleTemplate": "采购审批-{title}",
      "businessBinding": {
        "mode": "ADAPTER",
        "primaryKeyField": "id",
        "tenantField": "tenant_id",
        "titleField": "title",
        "statusField": "status",
        "ownerField": "applicantId"
      },
      "variableMapping": [
        {
          "formField": "businessKey",
          "flowVariable": "businessKey"
        }
      ]
    }

低代码配置步骤：

1. 创建业务对象
2. 发布业务对象运行配置
3. 创建业务应用
4. 将业务对象加入应用
5. 在应用中设计表单页面
6. 配置流程模型 Key
7. 配置业务绑定和状态字段
8. 在流程设计器中选择应用、对象和表单
9. 在节点中设置字段权限
10. 使用平台统一的 START_FLOW 动作发起流程

运行时接口：

    GET  /ai/business/flow/task-form-context
    GET  /ai/business/flow/task-form-context/readonly
    PUT  /ai/business/flow/task-form-context
    POST /ai/business/flow/task-action
    POST /ai/business/flow/start
    POST /ai/business/flow/resubmit

不建议在前端自定义动作中重复启动流程、只配置 nodeForms 不保存 BPMN 节点配置、把 configKey 当成 objectCode，或在列表循环中逐条请求业务详情。

## 十、审批动作二开

新增固定审批动作时：

1. 在任务 DTO 中增加明确字段
2. Controller 使用明确 DTO，不使用 @RequestBody Map
3. Service 增加动作白名单、权限校验和幂等校验
4. BPMN 节点配置增加动作开关
5. /api/flow/task/form/{taskId} 返回动作权限
6. 前端根据返回值展示按钮
7. 回调中维护业务状态
8. 增加重复提交和并发处理测试

当前审批请求字段包括：

    taskId
    comment
    signature
    targetActivityId
    directSend
    variables
    approvalPointResults
    idempotencyKey
    requestDigest
    tenantId

主要接口：

    POST /api/flow/task/approve
    POST /api/flow/task/reject
    POST /api/flow/task/reject-to-start
    POST /api/flow/task/return
    POST /api/flow/task/delegate
    POST /api/flow/task/reassign
    POST /api/flow/task/terminate
    POST /api/flow/task/withdraw

服务端会根据当前登录用户和租户重新校验身份，前端传入的 userId、tenantId 不能作为最终信任来源。

## 十一、常用接口索引

### 流程模型

    GET    /api/flow/model/page
    GET    /api/flow/model/{id}
    GET    /api/flow/model/key/{modelKey}
    GET    /api/flow/model/key/{modelKey}/start-config
    POST   /api/flow/model
    PUT    /api/flow/model
    DELETE /api/flow/model/{id}
    POST   /api/flow/model/{id}/deploy
    POST   /api/flow/model/{id}/suspend
    POST   /api/flow/model/{id}/activate
    POST   /api/flow/model/{id}/disable
    POST   /api/flow/model/{id}/enable

### 待办列表

    GET /api/flow/task/todo
    GET /api/flow/task/done
    GET /api/flow/task/started
    GET /api/flow/task/candidate

公共查询参数：

    pageNum
    pageSize
    userId
    title
    category
    status

当前已办 SQL 状态范围为 APPROVED、REJECTED、DELEGATED、CANCELED、RETURNED、TERMINATED。WITHDRAWN 当前不在已办列表查询范围内，如需展示已撤回任务，需要同步调整 FlowTaskStatus.DONE_CODES 和 Mapper XML。

## 十二、二开必须遵守的约束

- 复杂 SQL 写 Mapper XML，不在 Service 中用复杂 LambdaQueryWrapper
- 所有流程列表 SQL 明确带 tenant_id
- 业务内置数据 tenant_id 使用 1
- 雪花 ID 在前端和流程变量中使用字符串
- 业务状态使用枚举，不使用魔法数字和字符串
- 列表摘要必须批量查询，禁止逐条远程调用
- 流程模型已有非空 BPMN XML 时不得被初始化代码覆盖
- 节点表单和字段权限以 BPMN 配置为主
- Controller 固定字段使用 DTO，不使用整包 Map
- 审批、资金、状态变更等操作必须有幂等键
- 新增索引必须通过 Flyway
- 候选组查询必须校验当前租户、有效角色和当前组织
- 前端按钮隐藏不能替代服务端权限校验

最近定向验证结果：

- FlowTaskMapperSqlContractTest：2 个测试通过
- CountOnePaginationInnerInterceptorTest：1 个测试通过
- Flow 插件聚合编译通过
- ORM 模块编译通过
- 前端相关 Vitest 和生产构建已通过

部署后建议重点联调：

1. 业务应用 → 业务对象 → 表单资产的保存和回显
2. 发起测试表单渲染
3. 候选组待办权限
4. 跨租户数据隔离
5. 多级指定节点退回
6. 驳回至发起人后直送
7. 已办只读表单
8. 待办分页 COUNT
9. EXPLAIN 是否命中新建联合索引

